{
  "name": "core-showcase",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vanilla TS showcase for @omnisat/lasereyes-core — exercises createLaserEyesConfig + getWalletClient + connector.getClient overrides end-to-end.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@omnisat/lasereyes-client": "workspace:*",
    "@omnisat/lasereyes-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "^5.4.0"
  }
}
